version: "3.1"
services:
    db:
        image: postgres:15
        environment:
            - POSTGRES_PASSWORD=odoo
            - POSTGRES_USER=odoo
            - POSTGRES_DB=postgres
        volumes:
            - ./postgresql:/etc/postgresql
        command: -c 'config_file=/etc/postgresql/postgresql.conf'
        networks:
            - backend

    odoo16:
        image: xmars/vdx-vn:bep-hc
        depends_on:
            - db
        environment:
            - HOST=db
            - PORT=5432
            - USER=odoo
            - PASSWORD=odoo
        volumes:
            - ../../:/mnt/custom-addons # customize addons
            - ./etc:/etc/odoo
            - ./logs:/var/log/odoo
        networks:
            - backend

networks:
    backend:
        driver: bridge
